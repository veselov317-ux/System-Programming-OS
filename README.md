# System Programming OS — Jenkins, Docker, RPM, DEB, GitHub Actions

## Project Description

This project demonstrates a complete automation workflow including:
- creation of a Bash script,
- packaging the script into **RPM** and **DEB** packages,
- automated package build using **GitHub Actions**,
- running **Jenkins in Docker**,
- executing a Jenkins pipeline that installs a package and runs the script.

---